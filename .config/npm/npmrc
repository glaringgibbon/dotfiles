# Ensure globals go to your XDG_DATA_HOME (Option B we discussed)
prefix=/home/gg/.local/share/npm

# Ensure the cache is in the right place
cache=/home/gg/.cache/npm

# Professional defaults
init-author-name=Your Name
init-author-email=your-email@example.com
init-license=MIT
init-version=0.1.0

# Keep the terminal clean
loglevel=notice
progress=false
