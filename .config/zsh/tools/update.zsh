# Would this be a better location for the check language function?
# It is referrenced in the .zshrc adjacent to zprof and there is a file in tools folder called profile.zsh
# I am assuming that there may be a link between the profile.zsh file and zprof.
# Can you set options for profiling here or something?
