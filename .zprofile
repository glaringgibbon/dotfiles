#
# $HOME/.zprofile
#
# Login shell configuration
# HomeLab Dotfiles - Refactored 2026-01-18
#

# This file is for logic that should only run once upon login.
# .zshenv has already been sourced by the shell at this point.

# Example: Start a gpg-agent or ssh-agent if not running
# (We will move specific SSH/GPG logic to modules later)
